import React from 'react';
import ReactDOM from 'react-dom';
import Colors from './Colors';

const rootElement = document.querySelector('#root');
const element = <Colors />;

ReactDOM.render(element, rootElement);
